/**
 * The single source of truth for the version shown on the login page and
 * recorded against Update Logs entries.
 *
 * Semantic versioning: major (2.0.0) for breaking releases, feature (1.1.0) for
 * new functionality, patch (1.0.1) for fixes. Bump this BY HAND as part of
 * cutting a production release — never automatically on save or on a preview
 * deploy, or the number stops meaning anything.
 */
export const APP_VERSION = "1.1.0";

export const APP_NAME = "4S ROMA";
export const APP_FULL_NAME = "4S ROMA (Retention Order Management Application)";
